#include "main.h"
#include "_putchar.c"

/**
 * print_numbers - checks for checks for a digit (0 through 9).
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	 for (c = 48; c < 58; c++)
	 {
		 _putchar(c);
	 }
	 _putchar('\n');
}

